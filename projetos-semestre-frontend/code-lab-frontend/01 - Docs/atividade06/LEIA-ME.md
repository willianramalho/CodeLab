# O que colocar nesta pasta (atividade06)

`checkpoint-01.md` já está pronto. Com a API (`http://localhost:3000`) e o front
(`http://localhost:5173`) rodando, capture e salve aqui:

1. `registro-antes.jpg` — se você tiver um print antigo da tela de Registro sem estilo
   (de antes desta atividade), use-o. Se não tiver, pode pular este e anotar no envio —
   a versão sem estilo já não existe mais no código (foi substituída pela versão com
   `FormCard`/`BaseInput`/`BaseButton`).
2. `registro-depois.jpg` — a tela de Registro atual (`/register`), já usando os
   componentes-base e a cor de marca verde (`#198754`).
3. `tres-telas-consistentes.jpg` — Registro, Login (`/login`) e Meu Perfil
   (`/profile/me`) lado a lado (ou 3 abas), mostrando a consistência visual entre elas.
4. Um print da Navbar reagindo corretamente ao estado logado (mostra nome do usuário +
   "Meu Perfil" + "Sair") vs. deslogado (mostra "Entrar"/"Criar Conta").
5. Um print da Sidebar aparecendo só quando logado (some completamente quando deslogado).

Todos esses prints dependem de interação real no navegador — não foi possível gerá-los
nesta sessão.
